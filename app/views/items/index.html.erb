<h1>Items</h1>
<% if @items.any? %>
  <ul>
    <% @items.each do |item| %>
      <li><%= link_to item.name, item_path(item) %></li>
    <% end %>
  </ul>
<% end %>
<%= link_to "Add a new Item", new_item_path, class: "btn btn-block btn-primary" %>
